# Qwen 3.6 35B A3B MTP (MoE, IQ2_M) — tuned baseline for this MacBook Pro M5
# (16 GB unified memory, 4 performance cores, 10-core GPU).
# Sourced by server/start.sh. Override any VAR before sourcing, or edit here.

MODEL_KEY="qwen36-35b"
MODEL_LABEL="Qwen 3.6 35B A3B MTP (IQ2_M, multimodal, MoE)"

MODEL_PATH="${MODEL_PATH:-$MODELS_DIR/Qwen3.6-35B-A3B-MTP-GGUF/Qwen3.6-35B-A3B-UD-IQ2_M.gguf}"
MMPROJ_PATH="${MMPROJ_PATH:-$MODELS_DIR/Qwen3.6-35B-A3B-MTP-GGUF/mmproj-BF16.gguf}"
ENABLE_MMPROJ="${ENABLE_MMPROJ:-1}"
IMAGE_MIN_TOKENS="${IMAGE_MIN_TOKENS:-1024}"
ALIAS="${ALIAS:-Qwen3.6-35B-A3B-MTP-IQ2_M}"
PORT_DEFAULT="${PORT_DEFAULT:-8081}"

# long120k16 profile — 120 Ki-token window for long coding/agent sessions.
CTX_SIZE="${CTX_SIZE:-122880}"
THREADS="${THREADS:-$(sysctl -n hw.perflevel0.physicalcpu 2>/dev/null || echo 4)}"
THREADS_BATCH="${THREADS_BATCH:-$((THREADS * 2))}"
BATCH_SIZE="${BATCH_SIZE:-2048}"
UBATCH_SIZE="${UBATCH_SIZE:-1024}"
N_GPU_LAYERS="${N_GPU_LAYERS:-1}"
CACHE_TYPE_K="${CACHE_TYPE_K:-q4_0}"
CACHE_TYPE_V="${CACHE_TYPE_V:-q4_0}"
FLASH_ATTN="${FLASH_ATTN:-on}"
KV_OFFLOAD="${KV_OFFLOAD:-1}"
OP_OFFLOAD="${OP_OFFLOAD:-1}"
PARALLEL="${PARALLEL:-1}"
CACHE_RAM="${CACHE_RAM:-512}"
CACHE_REUSE="${CACHE_REUSE:-0}"
CTX_CHECKPOINTS="${CTX_CHECKPOINTS:-8}"
CHECKPOINT_MIN_STEP="${CHECKPOINT_MIN_STEP:-16384}"
FIT_TARGET="${FIT_TARGET:-2048}"

# Speculative decoding (MTP head is built-in; off by default on 16 GB).
# none | mtp
SPEC_MODE="${SPEC_MODE:-none}"
DRAFT_N_MAX="${DRAFT_N_MAX:-2}"
DRAFT_N_MIN="${DRAFT_N_MIN:-1}"
DRAFT_CACHE_TYPE_K="${DRAFT_CACHE_TYPE_K:-q4_0}"
DRAFT_CACHE_TYPE_V="${DRAFT_CACHE_TYPE_V:-q4_0}"

# Memory guard — refuses to start if free memory is low, to avoid display
# flicker / swap on this 16 GB Mac.
MEMORY_GUARD="${MEMORY_GUARD:-1}"
MIN_FREE_PCT="${MIN_FREE_PCT:-20}"

pre_launch() {
  if [[ "$MEMORY_GUARD" == 1 ]] && command -v memory_pressure >/dev/null 2>&1; then
    local free_pct
    free_pct="$(memory_pressure | awk '/System-wide memory free percentage/ { gsub(/%/, "", $5); print $5 }')"
    if [[ "$free_pct" =~ ^[0-9]+$ ]] && (( free_pct < MIN_FREE_PCT )); then
      echo "Memory guard: only ${free_pct}% free (< ${MIN_FREE_PCT}%). Refusing to start." >&2
      echo "Close apps or retry with MEMORY_GUARD=0." >&2
      exit 1
    fi
  fi
}

build_extra_args() {
  printf '%s\n' \
    --cpu-moe --jinja --cont-batching \
    --reasoning auto --reasoning-format deepseek \
    --cache-prompt --cache-idle-slots \
    --cache-reuse "$CACHE_REUSE" \
    --ctx-checkpoints "$CTX_CHECKPOINTS" --checkpoint-min-step "$CHECKPOINT_MIN_STEP" \
    --no-warmup --metrics \
    --fit on --fit-target "$FIT_TARGET" \
    --verbosity 3
}

build_spec_args() {
  case "$SPEC_MODE" in
    none) ;;
    mtp)
      printf '%s\n' \
        --spec-type draft-mtp \
        --spec-draft-n-max "$DRAFT_N_MAX" --spec-draft-n-min "$DRAFT_N_MIN" \
        --cache-type-k-draft "$DRAFT_CACHE_TYPE_K" \
        --cache-type-v-draft "$DRAFT_CACHE_TYPE_V"
      ;;
    *)
      echo "Qwen 3.6: SPEC_MODE must be none | mtp (got '$SPEC_MODE')" >&2
      exit 2
      ;;
  esac
}
